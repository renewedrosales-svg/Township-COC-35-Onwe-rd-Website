from .base import *

DEBUG = False

# Populated properly in Phase 16 (Production Deployment):
# ALLOWED_HOSTS, DATABASES (PostgreSQL), SECURE_* settings,
# STATIC/MEDIA via django-storages, email config.